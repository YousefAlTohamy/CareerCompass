# Legacy ai-job-miner Tests

These files preserve test intent from the earlier Smart AI Scraper architecture. They import old `core` and `pipeline` modules that are no longer part of the current service entrypoint, so they are stored here as reference material and named so pytest does not collect them automatically.

The maintained pytest suite for the current service is:

- `tests/test_ai.py`
- `tests/test_service_api.py`
